# 开源整理记录

## 已完成

- 增加项目 README、文档索引、贡献说明、测试状态和第三方来源清单。
- 按维护者选择添加 MIT 文本，明确第三方内容不被重新授权。
- 增加 `.gitignore`；将命中的历史编译输出、日志、波形、备份和附带桌面软件从 Git 索引移除，本地文件仍保留。
- `start.cmd` 改为从 `EFINITY_HOME` 或 `PATH` 查找工具，使用项目目录并透传退出码。
- 修正工程清单中的 I²C、锐化和 DPC 源文件路径。
- 移除未被顶层实例化的历史 AE 集成示例及两个缺失文件的综合条目。示例包含 testbench，且依赖不完整；保留原文件供参考，没有重写 RTL。
- 清除字模文本首行和尾部注释中的个人计算机绝对路径，保留字模数据。
- 增加可重复运行的工程完整性检查脚本。

## 发布前仍需完成

- [ ] 补齐第三方再分发依据及原创文件范围，见 [第三方说明](../THIRD_PARTY_NOTICES.md)。
- [ ] 在 Efinity 中完成综合、布局布线、时序检查与 bitstream 生成，记录工具版本和结果。
- [ ] 补充开发板版本、接线资料和摄像头 / 显示器配置，记录上板验证结果。
- [ ] 如需发布历史 AE 示例，补回缺失模块并验证仿真；否则继续标记为参考文件。
- [ ] 确定是否公开现有 Git 历史：停止跟踪只影响后续快照，旧提交仍含缓存、日志和附带软件。当前没有重写历史、创建提交或推送远端。

## 验证范围

本次检查结果：

- 153 个被忽略文件已停止跟踪，确认本地副本仍存在。
- `python scripts/check_project.py` 通过：101 个文件引用、19 个 include 目录。
- 新增 Markdown 文档的本地链接均存在。
- 构建入口使用模拟工具验证了 `EFINITY_HOME`、`PATH`、缺失工具提示、从其他目录启动和非零退出码传递；此项没有运行 Efinity。
- 工作区及索引的 `git diff --check` 通过。

工程文件检查仅说明清单中的文件存在并被跟踪，不说明 HDL 可以编译或达到时序要求。当前环境的 `PATH` 中未找到 Efinity、ModelSim 或 Icarus Verilog，因此本次不提供综合、仿真和上板通过的结论。

对当前源码、文档及配置做了常见令牌、私钥标记和个人绝对路径的基础扫描；这不覆盖所有二进制内容或历史提交，也不构成“无敏感信息”的保证。原作者联系信息作为来源声明保留。
