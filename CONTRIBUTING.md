# 贡献说明

提交问题时请附上器件和开发板型号、Efinity 版本、传感器与显示配置、复现步骤，以及相关错误日志片段。避免提交本机路径、凭据和整套编译输出。

修改 RTL 时说明所处时钟域、复位方式、输入输出格式及延迟变化。新增源文件后同步更新 `Ti60_Demo.xml`；不要把同名备选实现和 testbench 一并加入综合。

提交前运行 `python scripts/check_project.py`。如修改 HDL 或约束，还应运行相关仿真及 Efinity 构建，并在变更说明中列出实际完成的验证和未验证部分。涉及视频数据通路的改动应记录上板现象。

新增或引用第三方内容时保留作者和许可声明，并更新 `THIRD_PARTY_NOTICES.md`。仅对有权授权的原创贡献适用项目 MIT 许可。
